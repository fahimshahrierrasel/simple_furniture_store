name: demo_furniture_app
description: A new Flutter project from simple furniture app

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"


dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  google_fonts:
  sized_context:
  sliding_up_panel: ^2.0.0+1
  flutter_rating_bar: ^4.0.0
  styled_widget: ^0.4.0+3
  persistent_bottom_nav_bar: ^4.0.2
  smooth_page_indicator: ^1.0.0+2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/icons/
